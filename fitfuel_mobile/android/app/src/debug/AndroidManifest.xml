<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: allow cleartext (plain http://) traffic to the local
         CI/dev backend (10.0.2.2 / localhost). See
         res/xml/network_security_config.xml for why this is needed and
         why it's scoped to debug builds only. Merges onto the single
         <application> element from src/main/AndroidManifest.xml. -->
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
